server:
  port: 8086

spring:
  application:
    name: STORYBANK-INVESTMENT-ACCOUNT-SERVICE

  datasource:
    url: jdbc:mysql://localhost:3306/storyonjavabankinvest
    username: root
    password: 123sagar
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  jackson:serialization:INDENT_OUTPUT: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
